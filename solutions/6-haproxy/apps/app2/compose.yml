# APP2 : une application simple (nginx) livree par une AUTRE equipe dev.
# Le hostname du conteneur = la route (/app2) une fois declare dans l'annuaire ops.
services:
  app2:
    image: nginxdemos/hello:plain-text
    container_name: app2
    networks: [wpnet]
networks:
  wpnet:
    external: true
