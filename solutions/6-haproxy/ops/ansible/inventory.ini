# Inventaire OPS = backends récupérés de dev (ressources/inventory-backends.ini)
# + l'hôte "proxy" que l'ops administre.

[proxy]
proxy ansible_connection=community.docker.docker

# --- backends (handoff depuis dev) ---
[wordpress]
wp ansible_connection=community.docker.docker

[db]
db ansible_connection=community.docker.docker
