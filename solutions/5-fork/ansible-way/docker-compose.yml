services:
  db:
    image: mysql:8.0
    networks: [wpnet]
    environment:
      MYSQL_ROOT_PASSWORD: rootpw
      MYSQL_DATABASE: wordpress
  wordpress:
    image: wordpress:latest
    depends_on: [db]
    ports: ["8080:80"]
    networks: [wpnet]
networks:
  wpnet: {}
